# Google OAuth — NEVER commit these
token.json
client_secret.json
*.token.json

# Node
node_modules/

# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# Testing
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo

# E-ink output (local)
briefing_eink.json

# Superpowers brainstorm session state (local-only, never commit)
.superpowers/

# Local UI mockups (never commit)
preview-week-grid.html

# Playwright MCP browser session logs (local-only)
.playwright-mcp/

# Accidental nested repo snapshot (local-only)
/home-base/

# Private seed data — personal goals/notes that shouldn't enter public history.
# Apply locally via `supabase db query --linked --file <file>`.
supabase/*_private.sql

# Supabase CLI version-check cache — bumps every CLI run, no value in tracking.
supabase/.temp/cli-latest
